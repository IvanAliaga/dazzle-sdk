// Copyright 2026 Ivan Aliaga <ivan.aliaga@urp.edu.pe>
// SPDX-License-Identifier: Apache-2.0
//
// Re-exports for the three Dazzle Flutter samples. Matches the
// surface of samples/_shared/android and samples/_shared/ios so the
// sample's `main.dart` reads the same no matter which platform it
// targets.

library dazzle_samples_shared;

export 'src/chat_screen.dart';
export 'src/llm_adapter.dart';
export 'src/mini_embed.dart';
export 'src/sample_test_banner.dart';
export 'src/sample_test_runner.dart';
